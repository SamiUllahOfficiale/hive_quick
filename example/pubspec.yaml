name: example
description: Demonstrating how to use hive_quick.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_quick:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
